# Copy to .env and fill in.
OPENAI_API_KEY=sk-...

# Optional GitHub integration. Target repo metadata lives in sdlc-agent.yaml.
# GITHUB_TOKEN=

# Optional: parent directory for auto-cloned target repos when --target-repo-root is omitted (MCP mode).
# SDLC_TARGET_CLONE_PARENT=C:\path\to\clones
